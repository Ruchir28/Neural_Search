# Orchestrator Configuration
# Copy this file to orchestrator_config.env and fill in your actual values

# AWS Configuration
AWS_REGION=us-east-1
EBS_VOLUME_ID=vol-xxxxxxxxxxxxxxxxx  # Your EBS volume ID with embedding data

# EC2 Instance Configuration
EC2_INSTANCE_TYPE=g4dn.4xlarge  # GPU instance for CUDA workloads
EC2_AMI_ID=ami-0c02fb55956c7d316  # Amazon Linux 2 AMI (update for your region)
EC2_KEY_PAIR=semantic-search-key  # Your EC2 key pair name
EC2_SECURITY_GROUP=semantic-search-sg  # Security group allowing port 8000
EC2_SUBNET_ID=subnet-xxxxxxxxxxxxxxxxx  # Optional: specific subnet ID

# Instance Management
MAX_INSTANCES=1  # Maximum number of instances to run
INSTANCE_IDLE_TIMEOUT=1800  # Seconds before terminating idle instances (30 minutes)
HEALTH_CHECK_INTERVAL=60  # Seconds between health checks

# Repository Configuration (update the user data script with your actual repo)
GITHUB_REPO=https://github.com/your-username/your-repo.git
COHERE_API_KEY=your_cohere_api_key_here 